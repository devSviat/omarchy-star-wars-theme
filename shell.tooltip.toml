# Full [tooltip] section. Tooltips are small and short-lived, so they stay
# nearly solid for legibility.
background       = "#0d0e15"
background-alpha = 0.9
text             = "#dcd6e6"
border           = "hyprland.active-border-foreground"
border-alpha     = 1.0
